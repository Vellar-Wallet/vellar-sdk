# Connector Allowed-Origin Guard (#259)

A security policy enforcer for the `connector.ts` module to explicitly restrict the domains that are permitted to interact with the wallet SDK. This limits the attack surface against cross-origin invocation by malicious web apps.
