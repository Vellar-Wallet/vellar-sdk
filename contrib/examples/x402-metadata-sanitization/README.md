# Untrusted x402 Resource Metadata Sanitization (#261)

Demonstrates sanitization of resource-provided fields (like `name` and `description`) to prevent script injection and malformed metadata attempts. Output is guaranteed to be single-line plain text safe for rendering.
